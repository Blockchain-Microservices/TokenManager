CREATE TABLE IF NOT EXISTS "tokens" ("id" uuid NOT NULL UNIQUE DEFAULT gen_random_uuid (), "name" text NOT NULL, "symbol" text NOT NULL, "supply" integer NOT NULL, "decimals" integer NOT NULL, "address" text, "deployer" text, "txHash" text, "createdAt" text NOT NULL DEFAULT now(), "updatedAt" text NOT NULL DEFAULT now(), CONSTRAINT "PK_3001e89ada36263dabf1fb6210a" PRIMARY KEY ("id"));